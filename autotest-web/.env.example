# autotest-web (中心化) 环境变量
# cp .env.example .env && vim .env

# === 被测目标 ===
FRONTEND_URL=http://localhost:3000
API_BASE=http://localhost:4000

# === Lighthouse CI (可选，用来上传性能数据) ===
# LHCI_TOKEN=

# === Docker (dockle 需要) ===
# 确保 docker 可用: sudo usermod -aG docker ubuntu
